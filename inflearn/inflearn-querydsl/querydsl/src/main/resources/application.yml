spring:
  datasource:
    url: jdbc:h2:tcp:localhost/~/Desktop/study/SPAMMY_PLAYGROUND/inflearn/inflearn-querydsl/db/querydsl
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
#        show_sql: true
logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace

