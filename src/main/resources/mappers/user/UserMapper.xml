<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.seven.jong.repository.IUserMapper">

    <resultMap id="UserMap" type="com.seven.jong.VO.UserVO" >
        <id property="userId" column="user_id"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="name" column="name"/>
        <result property="birth" column="birth"/>
        <result property="phone" column="phone"/>
        <result property="regdate" column="regdate"/>
    </resultMap>
    
    <resultMap id="UserResponseMap" type="com.seven.jong.DTO.UserReponseDTO" >
        <id property="userId" column="user_id"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="name" column="name"/>
        <result property="birth" column="birth"/>
        <result property="phone" column="phone"/>
        <result property="regdate" column="regdate"/>
    </resultMap>
    

    <!-- 회원가입  -->
    <insert id="addUser">
        INSERT INTO AIR_USER (user_id, email, password, name, birth, phone)
        VALUES (AIR_USER_SEQ.nextval,#{email}, #{password}, #{name}, #{birth}, #{phone})
    </insert>
    
    <!-- 총 유저 수 가져오기 -->
    <select id="selectUserCount" resultType="Integer">
		select count(*) from air_user
	</select>
    
    <!-- 모든 유저 가져오기 -->
    <select id="userInfo" resultMap="UserResponseMap">
    	select * from air_user
    </select>
    
    
    <!-- 요청 페이지 유저 가져오기 -->
    <select id="pageUserInfo" resultMap="UserResponseMap">
    	select B.* from(select rownum rn, A.* from
		(select * from air_user order by regdate desc)A)B where rn between #{s} and #{e}
    </select>
    
    <!-- 하나의 유저 정보 가져오기 -->
    <select id="info" resultMap="UserResponseMap">
		select * from air_user where user_id=#{userId}
	</select>
	
	<!-- 유저정보 수정 -->
	<update id="updateUserInfo">
		UPDATE air_user SET NAME = #{name}, BIRTH = #{birth}, PHONE=#{phone} where USER_ID = #{userId}
	</update>

</mapper>

